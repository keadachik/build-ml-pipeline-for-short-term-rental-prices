name: eda
conda_env: conda.yml

entry_points:
  main:
    parameters:
      input_artifact: str
      output_artifact: str
      min_price: float
      max_price: float
    command: "python run.py {input_artifact} {output_artifact} {min_price} {max_price}"
